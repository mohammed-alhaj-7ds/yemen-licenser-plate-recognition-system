# Example: HTTPS server (use with Certbot).
# 1. certbot certonly --standalone -d domain.com -d api.domain.com
# 2. Add this server {} inside nginx.conf or include as separate file.
# 3. Redirect HTTP -> HTTPS in main server block.

# server {
#     listen 443 ssl http2;
#     server_name domain.com;
#     root /usr/share/nginx/html;
#     index index.html;
#
#     ssl_certificate     /etc/letsencrypt/live/domain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/domain.com/privkey.pem;
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
#     location /api/ {
#         proxy_pass http://backend:8000/api/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         client_max_body_size 500M;
#         proxy_read_timeout 600s;
#     }
#     location /media/ { proxy_pass http://backend:8000/media/; }
#     location /static/ { proxy_pass http://backend:8000/static/; }
# }
